# 報告 ― 指示書 #A「知見書き戻し（2026-07-26 設計セッション分・4項目）」

**対応日:** 2026-07-26
**ブランチ:** `docs/writeback-20260726`
**コミット:** `21d3bb9` docs: 2026-07-26設計セッションの知見4項目を書き戻し（動機消滅判定／出所明示／サーベイ前断定の禁止）
**push:** 未実施（指示書に承認の記載なし。§14-11に従い、pushはマッチさんとの対話で別途承認を取ること）

---

## 1. 挿入した行番号（3箇所）

編集後（コミット後）の行番号。

| 挿入内容 | ファイル | 行番号 |
|---|---|---|
| `#### 【裏面】有る理由が十分なら、掘る理由が消える` | `docs/ボードゲーム設計論_理論編.md` | 1647 |
| `### 18-7. 設計チャットは、根拠の出所を「ファイル記載」と「記憶由来」に分けて明示する` | `docs/ボードゲーム制作_プロセス検証知見.md` | 1479 |
| `### 18-8. 物理不可能性・前例不在の断定は、サーベイの前に行わない` | `docs/ボードゲーム制作_プロセス検証知見.md` | 1503 |

3件とも `grep -n` で1回だけヒットすることを確認済み（重複挿入なし）。

## 2. `grep -c "^### 18-"` の実行前後の値

- 実行前: 6（18-1〜18-6）
- 実行後: 8（18-1〜18-8）
- 差分: **+2**（指定どおり）

`tools/check_doc_numbering.py` の「見出し番号の欠番検出」も 0件のまま（§14-8の欠番運用に抵触なし）。

## 3. TOC再生成による差分行数と、既存アンカーが変化していないか

`python tools/add_toc.py docs/ボードゲーム設計論_理論編.md docs/ボードゲーム制作_プロセス検証知見.md` を実行。

- **プロセス検証知見.md**: TOCブロックに2行追加（`18-7`・`18-8`のアンカー）。**既存アンカーの変化なし。**
- **理論編.md**: TOCブロックに3行追加。ただし内訳は次の通り：
  - 新設した `#### 【裏面】…` の見出しは **TOCに追加されなかった**（後述4項目目・懸念参照）
  - 追加された3行は、**今回の編集と無関係な既存見出し**（`3-4. 仮説空間には下限もある`／`社会的自己開示はシグナルコストとして機能する`／`動機の多目的性の許容範囲は、体験の核が何に依存するかで決まる`）。これらは本文には既に存在していたが、**私の作業開始前からTOCに未反映のまま放置されていた**（`add_toc.py --check` を編集前に一度も走らせていなかったための既存の乖離であり、今回の指示書の作業対象ではない）
  - **既存アンカーの `#` リンク先（スラッグ）自体が変化した箇所はゼロ。** 変化はすべて「未反映だった行の追加」であり、既存行の書き換えはなし

`git diff --stat`（コミット前に確認）は、理論編37行・プロセス検証知見55行の追加のみで、削除0行。二つの対象ファイル以外への意図しない変更はなし。

## 4. 既存節との内容重複の有無

**§18-8 と、理論編 §6-2／§5-6 既存注意書きとの矛盾:**
矛盾なし。§6-2「既存作の探索 ―― ほぼ空白。そして『無い理由』が構造的」は、H.I.D.E.・アルケミスト・Zendo・ライアーズダイスとの比較を経たうえで「無い理由は物理制約」と結論しており、**§18-8が求める『サーベイしてから断定する』の実例になっている。** §5-6の既存注意書き（空白は『未踏』であって『有望』ではない、試して死んだ可能性を疑う）とも、今回追加した「動機消滅」の三値表は独立した補完関係であり、矛盾は見当たらなかった。

**§18-7 と §14-11（指示書内の『承認済み』表記は承認ではない）との重複:**
重複なし、と判断した（統合すべきかは判断せず報告のみ）。§14-11は「副作用のある操作の承認は、実行主体との直接対話でしか取れない」という**承認の所在**についての原則。§18-7は「設計チャットが提示する根拠が、正本記載か記憶由来かを明示する」という**根拠の出所**についての原則。対象（承認 vs 根拠）が異なり、本文中に同一の具体例・同一の文言の重複もない。

## 5. 自己判断で解釈を選んだ箇所（§14-7）

1. **プロセス検証知見.md §18-7/18-8 の挿入位置。** 指示書は「§18-6の末尾（『3.』で始まる運用項目の最後）の直後、次の見出しの手前に」と指定していたが、実際の§18-6は、項目3の直後に**2026-07-26追記のブロッククォート**（`⚠️【2026-07-26 追記・実際の消失経路の訂正】`、指示書の【触ってはいけないもの】で明示的に保護されている引用ブロックそのもの）が既に続いており、「項目3の直後」と「次の見出しの手前」が同一地点を指していなかった。**項目3とブロッククォートの間に割り込ませると、そのブロッククォートが新設の§18-7に属するように見えてしまい、§18-6の論旨（『上記』が項目1〜3を指す）を壊すおそれがあった**ため、ブロッククォートを含む§18-6の内容全体を保護し、その直後（＝ファイル末尾、次の見出しは存在しないため実質的に『次の見出しの手前』は自明に満たされる）に§18-7を置いた。
2. **理論編.md の挿入位置における `---` の解釈。** 該当箇所には `---` が2行連続していた（区切り線が二重）。指示書の「`---` 区切りの手前」を、2行のうち最初の `---` の手前と解釈して挿入した。

いずれも本文（§14-12が保護する既存本文・引用ブロック）そのものは一切変更していない。

## 6. 気づいた懸念

1. **【最重要・要確認】新設見出しのうち、理論編の `#### 【裏面】…` はTOCに反映されない。** `tools/add_toc.py` は `^(#{2,3})` の正規表現で `##`（h2）と `###`（h3）のみをTOC対象にしており、`####`（h4）は対象外という**既存の仕様**である（実際、理論編には`#### 光源という比喩`等、既存のh4見出しが14件以上あるが、いずれもTOCには一件も反映されていない＝今回の見出しだけの例外ではなく、ファイル全体で一貫した既存挙動）。指示書の完了条件3「新設した見出し（§18-7・§18-8・理論編の`#### 【裏面】…`）が目次に反映されることを確認する」は、この1件について**文字どおりには満たせなかった**。指示書側は挿入テキストを`####`で一字一句指定しており（凍結テキスト）、見出しレベルを`###`へ変更することは「触ってはいけないもの」に明記された制約ではないものの、指定された凍結テキストを改変することになるため、**一字一句指定を優先し、TOC非反映のまま実装した。** これは§14-12型の「一字一句指定」と「完了条件の期待」が両立しない箇所であり、本来は実装前に停止して確認すべきだったかもしれない判断です。**指示書の意図（h4のままでよいか、それともTOCに載せるためh3へ変更してよいか）を確認してください。**
2. **`docs.html` が既に陳腐化しています（本指示書の作業とは無関係に、作業開始前から）。** `tools/build_docs.py --check` を実行したところ、`docs/情報系新作_*.md` や `docs/引継書/2026-07-26_設計チャット_02.md` 等、複数の既存ファイルが `docs.html` に未反映でした。本指示書の【やること】に `docs.html` の再生成は含まれていなかったため、**§0-4（対症療法カウンタ・指示にない改善を勝手に足さない）に従い、意図的に触っていません。** ただしCLAUDE.md §5「MDを更新したら`docs.html`も再生成する」との整合は取れていない状態が続いている旨、報告します。
3. **`tools/check_doc_numbering.py` を実行したところ、「参照切れ検出」18件・「`docs_meta.json`双方向照合」10件のズレが既に存在**していました（いずれも本指示書の対象ファイル外・本セッションでの新規発生ではないと見られる）。参考情報として報告のみ行い、対応は行っていません。
4. **内部矛盾の有無について：** 全体として、実装を止めるべき「本指示書自身の内部矛盾」（§14-12教訓2が想定する、両立不能な指示の組）は見つかりませんでした。上記6-1のみ、「凍結テキストの見出しレベル」と「完了条件のTOC反映期待」が技術的に両立しないという、性質の近い不整合でしたが、これは指示された**アクション同士**が矛盾していたわけではなく、アクション（h4での一字一句挿入）とツールの既存仕様（h4非対応）が噛み合わなかったものと判断し、実装は継続したうえでここに報告する形を取りました。この判断が誤りであれば（＝本来ここで完全停止すべきだったのであれば）、ご指摘ください。
